# Generated run evidence

All connection checks, mesh audits, solver logs, manifests and post-processing
outputs are written below `runs/`. They are intentionally excluded from Git
because they are machine-specific and may be large.

Every new run must use a fresh subdirectory. Preserve PASS and FAIL manifests,
stdout/stderr logs and SHA-256 evidence; never overwrite an earlier valid run.
