# AGENTS.md

## 1. Mission

本仓库用于建立可复现的三维内外流一体化 CFD 自动化流程。

Python 是唯一总控层：
- Gmsh Python API 负责 STEP 导入、几何检查、边界识别和网格生成；
- SU2 CLI/MPI/CUDA 负责求解；
- ParaView pvbatch 负责无界面后处理；
- 禁止依赖 GUI 自动点击、录制宏或人工反复操作。

最终生产流程必须在没有 Codex 参与的情况下，通过命令行重复执行。

## 2. Sources of truth

处理任何任务前必须依次读取：

1. AGENTS.md
2. PLAN.md
3. config/project.toml
4. config/cases.csv
5. config/markers.toml（若已生成）

物理常数、工况、参考量和边界角色只能来自 config 目录。
不得在 Python、Jinja2 模板或 ParaView 脚本中重复硬编码项目常数。

## 3. Fixed engineering requirements

- 所有求解数据使用 SI 单位。
- STEP 原始单位为 mm，导入后转换为 m。
- 坐标系：+X 弹头到弹尾，+Y 向上，+Z 侧视图向外。
- 质心位置为 (2.6, 0, 0) m。
- 参考长度为 5.2 m。
- 参考面积为 0.2123716634 m^2。
- 使用美国标准大气生成静温、静压、密度、声速和黏度。
- 求解采用压缩性 RANS，基线湍流模型为 SST。
- 壁面为无滑移绝热壁面。
- 目标 y+ < 1。
- 边界层不少于 15 层；允许在尖锐边缘处停止、合并或缩减。
- 中网格目标为 800 万至 1200 万单元。
- 设计点必须进行粗、中、细三套网格的网格无关性验证。
- 不要求实验数据对比。
- 转弯段出口是内部测量面，不得设置成壁面或求解出口。
- 后部出口的边界条件未验证前，不得猜测静压或背压。

## 4. Mandatory operating policy

每个任务必须按照以下顺序执行：

1. 检查现状；
2. 在 PLAN.md 中写出本轮目标和验收标准；
3. 进行最小范围修改；
4. 运行短测试；
5. 检查日志和输出；
6. 更新 PLAN.md；
7. 汇总修改文件、执行命令、结果和剩余风险。

一次只推进一个阶段，不得跨越尚未通过的质量门。

未经用户明确批准，不得：

- 使用 sudo 安装、卸载或覆盖软件；
- 删除已有 SU2、Gmsh、ParaView 或 CUDA；
- 修改项目目录之外的文件；
- 执行 rm -rf、git reset --hard 等破坏性操作；
- 生成生产规模网格；
- 启动预计超过 15 分钟的计算；
- 同时启动多个占用同一 GPU 的算例。

原始 STEP 文件只读。
所有修复后几何必须写入 geometry/derived。

## 5. Geometry rules

几何导入后必须生成 surface_catalog.csv，至少包含：

- entity tag；
- STEP 名称；
- 面积；
- 质心；
- bounding box；
- 法向抽样；
- 相邻体；
- 推断边界角色；
- 识别置信度。

不得只依赖 Gmsh 数字 tag 识别边界。
markers.toml 应同时保存名称和几何特征。
若存在歧义，必须停止并报告，不得猜测。

几何修复必须在边界分类之前完成，因为修复可能改变 entity tag。

## 6. Mesh rules

禁止直接从 STEP 跳到约 1000 万网格。

必须依次完成：

- topology smoke mesh；
- 边界层局部试验；
- 小规模 RANS 试算；
- y+ 校核；
- 粗网格；
- 中网格；
- 细网格。

每套网格必须输出：

- 单元和节点数量；
- 各单元类型数量；
- 最小质量；
- 最大非正交性或等效指标；
- 负体积/负 Jacobian 数量；
- 各边界面数；
- 边界层实际层数和覆盖率；
- 网格文件哈希。

负体积或边界角色遗漏时不得启动 SU2。

## 7. Solver rules

首先用小网格验证边界条件和坐标符号。

正式 RANS 应采用两阶段策略：

1. 低 CFL、较稳健的一阶格式获得初始解；
2. 从 restart 继续二阶格式并完成收敛。

不得只根据残差判断收敛，还必须检查：

- 六分量力和力矩是否稳定；
- 内部截面流量和总压恢复是否稳定；
- 全局质量守恒；
- 出口是否存在回流；
- y+ 分布；
- 非物理解或 NaN；
- steady RANS 是否存在持续振荡。

若稳态量持续周期振荡，应停止并报告是否需要 URANS，不得直接取最后一步结果。

## 8. GPU rules

只有在以下证据同时存在时，才能声称使用了 GPU：

- SU2 构建记录显示 enable-cuda=true；
- case.cfg 中 ENABLE_CUDA=YES；
- 求解日志确认 CUDA 路径；
- nvidia-smi 或性能记录显示 GPU 活动；
- GPU 与 CPU 基准结果在数值容差内一致。

不得将 SU2 的 CUDA 线性求解器加速描述为整个 CFD 求解器完全运行在 GPU 上。

## 9. Post-processing rules

六分量载荷必须输出：

- Fx, Fy, Fz，正方向分别为 +X、+Y、+Z；
- Mx, My, Mz，关于 (2.6, 0, 0) m，采用右手定则；
- 同时输出系数和 N/N·m 量纲结果。

内部测量面必须输出：

- 净质量流量；
- 正向流出质量流量；
- 质量加权 Mach；
- 质量加权静压；
- 质量加权静温；
- 质量加权总压；
- 总压恢复系数；
- 截面面积；
- 回流比例。

所有后处理必须由 pvbatch 脚本完成并可重复运行。

## 10. Definition of done

某个阶段只有满足以下条件才能标记完成：

- 相关测试通过；
- 命令可从仓库根目录重复执行；
- 日志没有被忽略的错误；
- 输出文件路径明确；
- PLAN.md 已更新；
- Git diff 已检查；
- 结果附有配置和版本证据；
- 剩余假设和风险已明确列出。
