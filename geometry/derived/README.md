# Derived geometry

Generated or repaired CAD belongs below this directory and is excluded from
Git by default. The validated pipeline BREP path and SHA-256 are recorded in
`config/markers.toml`.

Recreate derived geometry through the documented `cfdpipe` quality gates, or
transfer the exact validated artifact through an approved private channel and
verify its digest before use. Do not substitute a similarly named file.
