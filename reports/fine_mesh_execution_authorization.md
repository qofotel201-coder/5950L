# 生产细网格生成授权

状态：`AUTHORIZED_NOT_BUILT`。粗—中系统加密一致性审计已PASS，因此允许在AutoDL生成约2000万单元细网格；合法校准范围为1600万至2400万。

唯一入口见同名JSON。执行时只替换一次`<UNIQUE_RUN_ID>`，保持同一BREP、marker、60层、首层高度、增长率、区域范围、Pilot修复参数及粗级`0.630`统一尺寸因子。生成结果通过独立质量与SU2读取门前禁止RANS。
